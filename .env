# Environment Variables for Booking Shooking

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000

# Database URL
# Replace with your actual database connection string
# For local development, you might use SQLite or a local PostgreSQL
# For production, use your cloud database URL
DATABASE_URL=postgresql://user:password@localhost:5432/booking_shooking

# Session Secret
# Generate a new random string for production
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-key-change-this-in-production

# Additional environment variables as needed
# Add any API keys, external service URLs, etc. here
